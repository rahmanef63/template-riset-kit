// The hero's ambient full-bleed background image (rendered behind the hero
// copy at low opacity in LandingRenderer's `hero` case). NOT a foreground
// card — the landing seed's `imageUrl` (which rendered a right-column card)
// was dropped so the image reads as background per design intent.
export const HERO_IMG = "/hero.webp";
